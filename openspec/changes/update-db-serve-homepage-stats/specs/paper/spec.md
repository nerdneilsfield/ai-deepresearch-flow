## ADDED Requirements

### Requirement: Homepage summary stats
The system SHALL display two summary rows on the papers homepage: overall totals and current filtered counts.

#### Scenario: Show total and filtered counts
- **WHEN** the homepage loads or filters change
- **THEN** the page shows totals and filtered counts for PDF/Source/Summary availability and template tags
