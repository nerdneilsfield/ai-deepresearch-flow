## 1. Implementation
- [x] 1.1 Build a linear doc+module task queue with shared concurrency control
- [x] 1.2 Persist per-module outputs immediately after module completion (stage + aggregate JSON)
- [x] 1.3 Load existing stage outputs and skip completed modules
- [x] 1.4 Add prompt hash tracking and invalidate stage output on mismatch
- [x] 1.5 Add CLI option to force re-run specific modules (e.g., --force-stage)
- [x] 1.6 Update CLI help/README to document resume behavior
