## 1. Extract rendering
- [ ] 1.1 Add extract flags to enable render output and configure template/dir.
- [ ] 1.2 Invoke render pipeline after extraction with the selected template.

## 2. Logging
- [ ] 2.1 Add tqdm progress for document and stage processing.
- [ ] 2.2 Add colored logs and a `--verbose` flag to control detail.

## 3. Documentation
- [ ] 3.1 Update README with the new extract render and logging flags.

## 4. Dependencies
- [ ] 4.1 Add `tqdm` and `coloredlogs` dependencies.
