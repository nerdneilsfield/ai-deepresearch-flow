## 1. Extract rendering
- [x] 1.1 Add extract flags to enable render output and configure template/dir.
- [x] 1.2 Invoke render pipeline after extraction with the selected template.

## 2. Logging
- [x] 2.1 Add tqdm progress for document and stage processing.
- [x] 2.2 Add colored logs and a `--verbose` flag to control detail.

## 3. Documentation
- [x] 3.1 Update README with the new extract render and logging flags.

## 4. Dependencies
- [x] 4.1 Add `tqdm` and `coloredlogs` dependencies.
