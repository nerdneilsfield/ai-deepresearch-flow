Template: seven_questions

{% if stage_name %}
Current stage: {{ stage_name }}.
Return JSON with keys: {{ stage_fields | join(", ") }}.
Previously completed outputs (JSON): {{ previous_outputs }}
{% else %}
Answer each question in the corresponding JSON field question_1 through question_7.
Use Markdown in each answer if helpful.
{% endif %}

Questions:
1) What problem does the paper try to solve?
2) What related research exists? (separate: strongest baseline, closest approach, orthogonal directions)
3) How does the paper solve the problem? (include a text flowchart)
4) What can be further explored? (split by theory/engineering/data/evaluation)
5) How would this work in real-world scenarios? (deployment constraints, compute, latency, robustness)
6) Summarize the main content (one paragraph + 5 bullets + one core formula/mechanism)
7) Which parts should readers focus on, and how should they read them? (newcomer/reproduction/review)

Document content:
{{ content }}

JSON Schema:
{{ schema }}
